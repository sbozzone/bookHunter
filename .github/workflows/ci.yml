name: CI

# Runs type-checking and a production build on every pull request and on pushes
# to the default branch, so broken changes can't merge silently.
on:
  pull_request:
  push:
    branches: [worked]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Production build
        run: npm run build
